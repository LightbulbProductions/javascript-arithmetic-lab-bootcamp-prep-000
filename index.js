1 + 80 // 81
60 - 40 // 20
2 * 3.4 // 6.8 
5.0 / 2.5 // 2
var number;
function add(a, b){ //add a and b and return the result.
  number = a + b;
  return number;
}
var number;
function subtract(a, b){ //add a and b and return the result.
  number = a - b;
  return number;
}

